---
export interface Props{
    className?:string
}

const { className } = Astro.props
---
<div class={`mx-auto max-w-screen-xl w-full px-5 sm:px-8 md:px-14 lg:px-10 ${className}`}>
    <slot/>
</div>
